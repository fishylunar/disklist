{
  "name": "disklist",
  "version": "1.1.2",
  "type": "module",
  "description": "Library to get disks and their info",
  "main": "dist/index.js",
  "scripts": {
    "test": "deno test --allow-env --allow-read --allow-run",
    "build": "rm -rf ./dist && deno run --allow-read --allow-write --allow-env --allow-net --allow-run bundle.ts",
    "start": "deno run --allow-env --allow-read --allow-run playground.ts"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fishylunar/disklist.js.git"
  },
  "author": "fishylunar",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/fishylunar/disklist.js/issues"
  },
  "homepage": "https://github.com/fishylunar/disklist.js#readme",
  "dependencies": {
    "esbuild": "^0.20.2",
    "eslint": "^9.13.0"
  }
}